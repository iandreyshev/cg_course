package ru.iandreyshev.cglab2_android.presentation.craft

import androidx.compose.ui.geometry.Size

const val ELEMENT_SIDE = 200f
val ELEMENT_SIZE = Size(ELEMENT_SIDE, ELEMENT_SIDE)

const val COMBINE_AREA_WIDTH = 100f
const val COMBINE_AREA_HEIGHT = 100f
val COMBINE_AREA_SIZE = Size(ELEMENT_SIDE, ELEMENT_SIDE)

const val COMBINE_AREA_W_PADDING = (ELEMENT_SIDE - COMBINE_AREA_WIDTH) / 2
const val COMBINE_AREA_H_PADDING = (ELEMENT_SIDE - COMBINE_AREA_HEIGHT) / 2

const val BIN_BOTTOM_MARGIN_DP = 32
const val BIN_RADIUS_PX = 96f

const val RANDOM_POS_MARGIN = 100

const val SELECT_ELEMENT_NAV_KEY = "select_element_key"