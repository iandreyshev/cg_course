package ru.iandreyshev.cglab2_android.presentation.common

import androidx.compose.ui.geometry.Size

const val ELEMENT_WIDTH = 200f
const val ELEMENT_HEIGHT = 200f
val ELEMENT_SIZE = Size(ELEMENT_WIDTH, ELEMENT_HEIGHT)

const val COMBINE_AREA_WIDTH = 100f
const val COMBINE_AREA_HEIGHT = 100f
val COMBINE_AREA_SIZE = Size(ELEMENT_WIDTH, ELEMENT_HEIGHT)

const val COMBINE_AREA_W_PADDING = (ELEMENT_WIDTH - COMBINE_AREA_WIDTH) / 2
const val COMBINE_AREA_H_PADDING = (ELEMENT_HEIGHT - COMBINE_AREA_HEIGHT) / 2
